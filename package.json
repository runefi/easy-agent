{
  "name": "easyagent",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "start": "turbo start",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules",
    "ui:add": "pnpm --filter @easyagent/ui ui:add"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@types/jest": "^29.5.14",
    "ai": "^4.3.4"
  }
}
